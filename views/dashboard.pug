doctype html
html
  head
    title Video Dashboard
  body
    h1 Hello #{user.name}, welcome to your dashboard
    each video in videos
      div
        h3 #{video.title}
        iframe(width='300' height='200' src=video.url frameborder='0' allowfullscreen)
